<Window x:Class="FacebookCore.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FacebookCore.Views"
        xmlns:VM="clr-namespace:FacebookCore.ViewModels"
        xmlns:model ="clr-namespace:FacebookCore.Models"
        mc:Ignorable="d"
        Title="{Binding WndTitle}" Height="450" Width="800">
    <Window.DataContext>
        <VM:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Button Content="Button" Command="{Binding TestCommand}" />
    </Grid>
</Window>

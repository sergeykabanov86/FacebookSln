<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                
                    xmlns:sys="clr-namespace:System;assembly=System.Runtime"
                    >
    <!--xmlns:decanat="clr-namespace:FacebookMySDKCore.Models.Decanat"
    <DataTemplate DataType="{x:Type decanat:Group}">
        <TextBlock Text="{Binding Name}" Background="LightBlue" HorizontalAlignment="Stretch"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type decanat:Student}">
        <TextBlock Text="{Binding Name}"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type sys:String}">
        <TextBlock>
                   <Run Foreground="Red">Строка: </Run> <TextBlock Text="{Binding}"/>
        </TextBlock>
    </DataTemplate>-->
</ResourceDictionary>